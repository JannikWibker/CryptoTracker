{
  "private": true,
  "name": "cryptotracker",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "start": "if-env NODE_ENV=production && npm run -s serve || npm run -s dev",
    "build": "preact build",
    "serve": "preact build && preact serve",
    "serve_only": "preact serve",
    "dev": "preact watch",
    "lint": "eslint src"
  },
  "eslintConfig": {
    "extends": "eslint-config-synacor"
  },
  "eslintIgnore": [
    "build/*"
  ],
  "devDependencies": {
    "eslint": "^4.19.1",
    "eslint-config-synacor": "^2.1.0",
    "if-env": "^1.0.4",
    "preact-cli": "^2.2.1"
  },
  "dependencies": {
    "js-yaml": "^3.13.1",
    "lodash": "^4.17.11",
    "mime": "^2.4.4",
    "preact": "^8.4.2",
    "preact-compat": "^3.19.0",
    "preact-redux": "^2.1.0",
    "preact-router": "^2.6.1",
    "redux": "^3.7.2",
    "redux-promise": "^0.5.3",
    "redux-thunk": "^2.2.0",
    "webpack-dev-server": "^3.7.2"
  }
}
